# 作品名称

**STM32G431 磁场定向控制单轴云台姿态自稳系统**

（不要出现任何涉及学校名称和指导老师等内容）

---

## 摘要（800字内）

本项目面向工业现场对轻载姿态保持与小功率伺服的需求，设计并实现了一套基于 STM32G431CBT6 的单轴云台磁场定向控制（FOC）自稳系统。系统以 GM3506 无刷云台电机为执行机构，配合 ATK-PD6010B 三相驱动、AS5048A 磁编码器与 MPU6050 惯性测量单元，在 12 V 直流供电下完成电角度校准、闭环速度/位置控制及 IMU 辅助水平自稳。

软件采用“模型设计 + 嵌入式部署”路线：电流环 PI 与 SVPWM 由 Simulink Embedded Coder 自动生成并在 20 kHz PWM-ADC 同步中断中执行；位置环与自稳逻辑在应用层以约 1 kHz 更新，采用分段比例增益、微分阻尼与积分项消除重力静差。系统具备 ALIGN 旋转式零位校准、串口命令调度、100 Hz 遥测及母线电压/ADC 饱和等软件保护，上位机通过 Python 脚本实现一键流程与看门狗。

经仿真与实物验证，电机可按指令正反转（如 ±40°/s、±100°/s），在底座倾斜时进入自稳模式可保持负载臂相对水平。本作品将电机控制、传感器融合与工程化工具链结合，具有结构清晰、参数可追溯、便于扩展多轴与视觉伺服等特点，适用于巡检云台、小型机器人关节及教学科研中的 FOC 实践平台。

**【插图建议】** 系统实物斜 45° 照片；README 中控制架构框图（`foc_verified_final.png` 或 `foc_closedloop.png`）。

---

# 第一部分  作品概述

## 功能与特性（400字内）

本系统实现单轴云台的 FOC 驱动与姿态自稳。主要功能包括：（1）**电角度校准（ALIGN）**：开环采样结合 π/2 修正与 Iq 脉冲方向校验，建立可靠 dq 坐标；（2）**运行模式**：使能 RUN 后支持速度指令（S\<dps\>）、力矩注入（I\<A\>）、开环换相测试（O/X）及零点设定（H）；（3）**IMU 自稳（G）**：将 MPU6050 俯仰角映射为位置目标，电机反向补偿使臂端保持水平；（4）**安全与诊断**：母线 8–26 V 监测、ADC 饱和检测、速度超限切断、串口遥测与 dq 统计。

硬件上 MCU 170 MHz Cortex-M4 驱动 20 kHz 电流环；编码器 14 bit SPI 绝对角；通信采用 UART 字符协议，PC 端 `foc_ctl.py` 可脚本化演示全流程。

**【插图建议】** 功能演示命令表截图（README）；自稳前后对比照片。

## 应用领域（400字内）

**工业 4.0 与机器人**：单轴稳定平台可用于轻量传感器载体、管道/廊道巡检相机云台、协作机械臂末端调姿等，在载体晃动时维持视轴或工具姿态。**无人机与光电吊舱**：原理可扩展至多轴万向节，为航拍、搜救提供稳像基础。**新能源与装备测试**：FOC 平台可用于电机参数辨识、驱动算法对比及 HIL 前的快速原型。**教育与竞赛**：覆盖 CubeMX 外设、Simulink 代码生成、CMake 交叉编译与现场调试，契合嵌入式芯片与电机控制选题。

本方案强调低成本商用模块（GM3506、PD6010B、磁编）与开源工具链，便于实验室复现与二次开发。

**【插图建议】** 应用场景示意图（巡检/稳像示意，可手绘）。

## 主要技术特点（400字内）

（1）**分层控制架构**：电流环 20 kHz（Simulink 生成 PI + SVPWM），位置/自稳环 1 kHz（非线性 KP、KD、KI），职责清晰、易于调参。（2）**模型驱动开发**：`foc_controller.slx` 与 `foc_params.m` 统一带宽（200 Hz）与限幅，生成代码与 `foc_interface.c` ISR 无缝对接。（3）**高精度角度链**：AS5048A SPI 绝对编码 + 旋转校准，消除安装误差；IMU 提供重力参考实现外环补偿。（4）**工程化状态机**：INIT→CALIBRATE→READY→RUN/FAULT，含零偏采集、对齐验证与故障复位策略。（5）**可观测性**：100 Hz 遥测、Simulink 闭环仿真（`foc_sim.slx`）及 Python 监控，缩短“仿真—下位机”迭代周期。（6）**机械适配**：`gimbal_mount/` 提供 3D 打印支架 STL，便于快速搭建单轴演示台。

## 主要性能指标（200字内，建议用表格）

| 指标项 | 参数 |
|--------|------|
| 主控 MCU | STM32G431CBT6，170 MHz |
| 电机 | GM3506，11 极对 |
| 电流环执行频率 | 20 kHz |
| 电流环设计带宽 | 200 Hz（Simulink PI） |
| 编码器 | AS5048A，14 bit SPI |
| IMU | MPU6050，I2C |
| 供电 | 12 V DC，母线保护 8–26 V |
| 速度模式 | 指令示例 ±40°/s、±100°/s |
| 速度保护 | 400°/s（ISR 切断力矩） |
| 位置环输出限幅 | 约 45°/s（应用层） |
| 遥测 | UART 100 Hz |

## 主要创新点（200字以内，逐点给出）

1. **Simulink 电流环 + ISR 位置自稳**：内环模型自动生成，外环针对重力静差定制分段 KP 与积分，兼顾响应与平稳。  
2. **旋转式 ALIGN 流程**：结合开环角与编码器互校验，降低磁编安装误差对 FOC 的影响。  
3. **IMU—编码器双反馈自稳**：俯仰角作位置目标，编码器闭环执行，底座倾斜时仍保持水平。  
4. **脚本化演示与看门狗**：`foc_ctl.py` 一键串联校准、使能、自稳，便于竞赛现场复现。  
5. **完整开源链路**：CubeMX + CMake + Simulink ERT + 仿真图，参数与代码同源可追溯。

## 设计流程（200字内）

需求分析（单轴稳像、FOC）→ 电机与驱动选型 → Simulink 电流环建模与 `foc_sim.slx` 闭环验证 → CubeMX 配置 TIM1/ADC/SPI/I2C/UART → 集成 ERT 代码与 `app_control` 状态机 → 编码器/IMU 驱动与 ALIGN → 位置与自稳联调 → 3D 打印机械件装配 → 串口协议与 Python 工具 → 实物指标测试与文档整理。

**【插图建议】** 设计流程框图（6–8 步）。

---

# 第二部分  系统组成及功能说明

## 整体介绍

系统由**功率与驱动**、**传感**、**控制计算**、**人机交互**四部分组成。12 V 经驱动板 ATK-PD6010B 逆变驱动 GM3506；STM32G431 通过 TIM1 输出 SVPWM，ADC 同步采样三相电流与母线/温度；AS5048A 经 SPI 提供转子位置，MPU6050 经 I2C 提供姿态；UART 连接 PC 下发 a/e/H/G/S 等命令并上传遥测。

**数据流**：IMU pitch / 用户指令 → 位置目标 → ISR 级 PID → Iq 参考 → Simulink 电流 PI → SVPWM → 电机；编码器角度反馈至位置环与 FOC 电角度。请在正文插入系统框图（可参考 README 架构图并标注各模块与信号线）。

## 硬件系统介绍

### 2.2.1 硬件整体介绍

核心为 STM32G431CBT6 最小系统 + ATK-PD6010B 三相桥 + GM3506 电机；传感包括 AS5048A（轴端磁编）与 MPU6050（IMU 模块）；电源 12 V，逻辑 3.3 V。MCU 负责 PWM、电流采样、通信与实时算法；驱动板完成功率放大与保护。整体为桌面级单轴演示平台，便于示波器测电流与串口观察状态。

**【插图】** 硬件实物全景、模块连接照片。

### 2.2.2 机械设计介绍

采用 `gimbal_mount/` 中 3D 打印支架固定电机、IMU 与负载臂，保证 IMU 与旋转轴关系稳定、负载质心接近轴心以减小重力矩。设计从**总成布局**（底座—电机—臂—传感器）到**紧固与走线**逐级说明，并给出 CAD/STL 渲染或实物照片。若使用手绘草图，需标注轴系与 IMU 安装方向。

### 2.2.3 电路各模块介绍

**总体**：MCU 板—驱动板—电机三相线；SPI 接编码器，I2C 接 IMU，UART 接 USB 转串口；ADC 采电流与母线。**分区说明**：（1）功率与驱动 ATK-PD6010B；（2）MCU 最小系统与时钟；（3）电流采样与运放通道（标注 IU/IV/IW、VBUS）；（4）编码器 SPI（CLK/MISO/MOSI/CS）；（5）IMU I2C（SCL/SDA）。请插入 `原理图&封装/` 中 SCH、PCB 截图，并用箭头标出关键信号走向。

## 软件系统介绍

### 2.3.1 软件整体介绍

软件分**下位机固件**（`STM32G431CBT6_Motor/`）与**上位机工具**（`tmp/foc_ctl.py`、`foc_monitor.py`）及**离线仿真**（MATLAB/Simulink）。固件基于 HAL + CMake 构建；应用层 `app_control.c` 状态机，`foc_interface.c` 负责 ISR 内测量、电流环 step 与保护；Simulink 生成 `foc_controller.c`。PC 端通过串口发送单字符命令并可选绘制遥测曲线。

**【插图】** Simulink 模型截图、串口工具运行界面、`foc_closedloop.png` 仿真结果。

### 2.3.2 软件各模块介绍

| 模块 | 文件 | 功能要点 |
|------|------|----------|
| 状态机与模式 | `app_control.c` | INIT/CALIBRATE/READY/RUN/FAULT；速度/位置/自稳；ALIGN |
| FOC 接口 | `foc_interface.c` | ADC→物理量；20 kHz 调用 foc_controller_step；开环调试 |
| 电流环 | `foc_controller.c`（ERT） | Id/Iq PI，SVPWM 占空比输出 |
| 编码器 | `encoder_spi.c` | AS5048A 读角、速度估计 |
| IMU | `mpu6050.c` | 俯仰角、自稳目标 |
| 遥测 | `telemetry.c` | 100 Hz 帧上传 |
| 上位机 | `foc_ctl.py` | 命令序列与 watchdog |

**流程说明（文字级）**：`main` 初始化外设 → 校准零偏 → 主循环 `app_control_update` 读 IMU/故障 → PWM ISR 内 `foc_interface_isr` 更新电流环并读编码器参与位置 PID。关键变量：`iq_ref`、`theta_e`、位置误差 `deg`、IMU pitch。可补充各函数流程图（draw.io/Visio）。

---

# 第三部分  完成情况及性能参数

## 整体介绍

请插入**系统实物正面**与**斜 45° 全局**照片各一张，展示电机、驱动、主控、负载臂与线束整理效果。

## 工程成果

### 3.2.1 机械成果

3D 打印支架装配完成，电机轴与负载臂同轴度满足演示要求；自稳测试时底座可手动倾斜约 45°，臂端仍趋向水平（请附照片/短视频截图）。

### 3.2.2 电路成果

驱动板与 MCU 联调通过，三相 PWM 与电流采样正常；SPI 编码器、I2C IMU 通信稳定。附 PCB 焊接面/正面及上电指示灯照片。

### 3.2.3 软件成果

附串口终端执行 `a→e→H→G` 或 `foc_ctl.py run "..."` 的截图；Simulink 仿真波形（`foc_verified_final.png`）；可选 CMake 编译成功与烧录日志。

## 特性成果（量化指标）

| 验证项 | 方法 | 结果 |
|--------|------|------|
| 速度跟踪 | 命令 S40/S-40/S100/S0 | 方向与转速可跟随指令 |
| 自稳 | 底座倾斜 + G 模式 | 负载臂保持水平趋势 |
| 电流环 | 示波器/遥测 Iq | 20 kHz 稳定运行 |
| 保护 | 低母线/ADC 异常 | 进入 FAULT 并停机 |

**【插图】** 示波器电流波形、自稳现场照片、仿真对比图。

---

# 第四部分  总结

## 可扩展之处（300字内）

可扩展为**双轴/三轴云台**，增加第二路 FOC 与 yaw 轴编码器；引入 **CAN/EtherCAT** 替代 UART 便于工业组网；在位置环外增加 **视觉伺服**（OpenMV/树莓派）实现目标跟踪；对 `foc_params.m` 做 **在线参数辨识** 适配不同电机；增加 **FreRTOS** 划分通信与日志任务；机械上采用 CNC 支架提高刚度；安全方面完善编码器去抖、过温与硬件急停，满足更长时运行。

## 心得体会（1000字内）

本作品从选题、仿真到下位机联调，完整走通了 STM32G4 电机控制链路。初期在电流采样符号、相序与 ADC 通道映射上花费较多时间，通过 `FOC_DBG_FIXED_VECTOR` 等分级调试手段逐相验证，体会到“先开环、再闭环、最后上负载”的必要性。Simulink 生成代码显著降低手写 PI 与限幅错误，但 ISR 时序与全局变量接口仍需严格约定，否则会出现积分饱和或抖动。

ALIGN 与磁编安装高度相关，旋转校准 + 手动 F 微调命令提高了现场适应性。自稳模式将 IMU 与位置环结合，重力矩由积分项消化，分段 KP 则在远近误差下平衡快速性与平稳性。上位机脚本化演示极大方便竞赛展示，也暴露协议需防误触与看门狗的必要性。

机械结构虽为 3D 打印，质心与轴系对齐对自稳效果影响明显，后续应在 CAD 阶段做简单动力学估算。整体而言，项目整合了意法半导体 MCU、FOC 算法、传感器融合与工程工具链，为工业 4.0 电机控制场景提供了可复现的原型平台；后续将在多轴协调、参数自整定与更长可靠性测试上继续完善。

---

# 第五部分  参考文献（20篇以内）

[1] TI. Field Oriented Control of 3-Phase AC Motors. Literature Number: SPRU723, 2013.  
[2] ST Microelectronics. STM32G431xx Datasheet and Reference Manual, 2024.  
[3] ST Microelectronics. AN1076: Sensorless Field Oriented Control of PMSM Motors.  
[4] ST Microelectronics. AN4680: Field-oriented control of PMSM driven by sensorless algorithm.  
[5] MathWorks. Embedded Coder User's Guide, R2024b.  
[6] ams OSRAM. AS5048A Magnetic Position Sensor Datasheet.  
[7] InvenSense. MPU-6000/6050 Product Specification.  
[8] Bose B K. Modern Power Electronics and AC Drives. Prentice Hall, 2002.  
[9] Mohan N. Power Electronics: A First Course. Wiley, 2012.  
[10] 王成元, 夏加宽, 杨俊友. 电机现代控制技术. 机械工业出版社.  
[11] 陈伯时. 电力拖动自动控制系统——运动控制系统. 机械工业出版社.  
[12] 全国大学生嵌入式芯片与系统设计竞赛 2026 竞赛指南（意法半导体赛道）.  
[13] ARM Ltd. CMSIS-DSP Software Library Documentation.  
[14] STM32CubeG4 User Manual: TIM PWM Generation and ADC Injection.  
[15] Atkinson K E. Numerical Analysis for SVPWM Modulation Principles (textbooks).  
[16] Python Software Foundation. pyserial Documentation.  
[17] GitHub. STM32 HAL Driver Overview and Best Practices.  
[18] J. Wang et al. Permanent Magnet Synchronous Machine Model for Field-Oriented Control. IEEE.  
[19] ISO/IEC/IEEE 12207:2017 Systems and software engineering — Software life cycle processes.  
[20] MIT License Open Source Software Licensing (project README).

---

**说明**：请将文中【插图建议】替换为实际照片/截图后，复制到原 Word 模板对应章节。勿在正式稿中保留学校名、指导教师姓名。